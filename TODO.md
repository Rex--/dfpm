dfpm TODO
=========

- [ ] Flags:
    - `-p, --pack-dir` specify alternate pack directory (default: `~/.mchp_packs`)
    - `-d, --download` download packs automatically (like when searching or updating)
    